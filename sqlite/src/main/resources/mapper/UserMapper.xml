<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shui.sqlite.mapper.UserMapper">

    <select id="findUser" resultType="com.shui.sqlite.data.User" parameterType="string">
        select
              *
        from
              user
        where
              id = #{id}
    </select>

</mapper>